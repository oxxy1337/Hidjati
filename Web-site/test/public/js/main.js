/*var scroll= window.requestAnimationFrame || function(callback){window.setTimeout(callback,100/60)};
var elementsToShow = document.querySelectorAll('.main');

function loop(){
  elementsToShow.forEach(function(element){
    if(isElementInViewport(elemnt)){
      elemnt.classList.add('is-visible');}
    else{
     elemnt.classList.remove('is-visible');
    
    }
  })

  scroll(loop);
}
loop();
*/

